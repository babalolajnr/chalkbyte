# Cargo configuration for Chalkbyte

# SQLx compile-time verification
# Set SQLX_OFFLINE=true to skip database connection during compilation
# Useful when database is not available or for faster builds
[env]
# SQLX_OFFLINE = "true"

# Uncomment above line to enable offline mode by default
# Or set environment variable: export SQLX_OFFLINE=true

# Note: Offline mode requires sqlx-data.json generated by:
# cargo sqlx prepare --database-url "postgresql://postgres:postgres@localhost:5432/chalkbyte_test"
